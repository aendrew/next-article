const cheerio = require('cheerio');
const relatedBoxExpanderTransform = require('../../../server/transforms/related-article-to-teaser');
require('chai').should();

describe('Related Box Expander Transform', () => {

	context('Related articles', () => {

	});
});
